# Gemini engine config (Vertex AI)
# Copy to .env.gemini and fill in your values

VERTEX_PROJECT_ID=your-project-id
VERTEX_LOCATION=global
VERTEX_SA_KEY_PATH=/path/to/service-account.json
GCS_BUCKET_NAME=your-audit-bucket
GEMINI_MODEL=gemini-3-flash-preview

# Shared data root (enterprise PDFs, enterprise list)
# DCMM_DATA_ROOT=/Users/shanfu/Desktop/audit_batch
